import Link from 'next/link';import { StatCard } from '@/components/StatCard';import { getStats } from '@/lib/data';
export default async function Admin(){const s=await getStats(); const stats=[['Total leads',s.totalLeads],['Qualified leads',s.qualifiedLeads],['Booked estimates',s.bookedEstimates],['Show rate',`${s.showRate}%`],['No-show rate',`${s.noShowRate}%`],['Closed jobs',s.closedJobs],['Cost per booked estimate',s.costPerBookedEstimate],['Revenue won',s.revenueWon]];return <main className="mx-auto max-w-7xl px-6 py-10"><div className="flex items-center justify-between"><h1 className="text-4xl font-black">Contractor dashboard</h1><div className="flex gap-3"><Link className="btn-secondary" href="/admin/leads">Leads</Link><Link className="btn-secondary" href="/admin/appointments">Appointments</Link><Link className="btn-secondary" href="/admin/settings">Settings</Link></div></div><div className="mt-8 grid gap-4 md:grid-cols-4">{stats.map(([l,v])=><StatCard key={l} label={String(l)} value={v}/>)}</div></main>}
